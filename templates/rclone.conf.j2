[gdrive]
type = drive
scope = drive
# Use your own client ID and secret from Google Cloud Console OAuth credentials
# Leave blank to use rclone's built-in credentials (works but has rate limits)
client_id =
client_secret =
# The token will be automatically added here after running 'rclone config' interactively
token =
